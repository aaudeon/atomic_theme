{#
/**
 * @input
 * Theme override for a input tel
 *
 * Available variables:
 * - idName : ID to bind the label and the input field.
 * - name: Name to the field.
 * - value: Defualt value.
 * - label: The title of the field.
 *
 */
#}

<div class="input-field niji-tel">

    <label for="{{ idName }}">{{ label }}</label>

    <input type="tel" name="{{ name }}" id="{{ idName }}" />

</div>
